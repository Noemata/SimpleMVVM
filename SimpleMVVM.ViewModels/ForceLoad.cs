namespace SimpleMVVM.ViewModels
{
    // MP! resolve: A better way to force an assembly load that needs type resolution before its used.
    //              Look at the constructor of App.xaml.cs to see how this is used.
    public static class SimpleMVVM_ViewModels_ForceLoad
    {
    }
}

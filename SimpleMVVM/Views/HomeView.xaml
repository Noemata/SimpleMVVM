<Page
    x:Class="SimpleMVVM.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleMVVM.Views"
    xmlns:viewModels="using:SimpleMVVM.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewModels:HomeViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Grid>
        <TextBlock Text="{x:Bind ViewModel.Message, Mode=OneTime}" FontSize="44" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Button Content="Get Busy"
                Margin="20"
                FontSize="22"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Command="{x:Bind ViewModel.GetBusyAsyncCommand}" />
    </Grid>
</Page>
